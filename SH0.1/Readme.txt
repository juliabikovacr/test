To run the project:
1) You should run script SH.DAL/SQL/main.sql on your SQL Server (creates the DB)
2) Insert the connection string of created DB into app.config file located in SH.DAL intead of [CONNECTIONSTRING].